<div class="navbar navbar-inverse navbar-static-top">
  <div class="navbar-inner">
    <div class="container pull-left">
      <ul class='nav'>
        <a href="#" class="brand pull-left">flickrClone</a>
        
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" 
              class="dropdown-toggle"
              data-toggle="dropdown">You</a>
            <ul class="dropdown-menu" role="menu">
              <li>
                <%= link_to 'Photostream', user_photos_url(current_user) %>
              </li>
              <li>
                <%= link_to 'Albums', user_albums_url(current_user) %>
              </li>
              <li>
                <%= link_to 'Create Album', new_album_url %>
              </li>
              <li>
                <%= link_to 'Groups', user_groups_url(current_user) %>
              </li>
            </ul>
          </li>
          <li class="dropdown">
            <a href="#"
              class="dropdown-toggle"
              data-toggle="dropdown">Contacts</a>
            <ul class="dropdown-menu" role="menu">
              <li>
                <%= link_to 'Recent Photos', friends_photos_url %>
              </li>
              <li>
                <%= link_to 'Contact List', friends_url %>
              </li>
            </ul>
          </li>
        <% end%>
        
        <li class="dropdown">
          <a href="#" 
            class="dropdown-toggle"
            data-toggle="dropdown">Groups</a>
          <ul class="dropdown-menu" role="menu">
            <li>
              <%= link_to 'All Groups', groups_url %>
            </li>
            <li>
              <%= link_to 'Create Group', new_group_url %>
            </li>
          </ul>
        </li>
        <li><%= link_to 'Upload', new_photo_url %></li>
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#"
            class="dropdown-toggle"
            data-toggle="dropdown">Signed in as <%= current_user.name %></a>
            <ul class="dropdown-menu" role="menu">
              <li>
                <%= button_to "Sign Out",
                  destroy_user_session_url,
                  :method => :delete,
                  class: 'btn btn-primary btn-block' %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "Sign In", new_user_session_url %></li>
          <li><%= link_to "Sign Up", new_user_registration_url %></li>
        <% end %>
      </ul>   
    </div>
  </div>
</div>